out/
__pycache__/
# autonomous-run Datom log (regenerated by methods/autorun.py — append-only local commit-DAG)
data/persisted/danjo.datoms.kotoba.edn
# the three other append-only local Datom logs the R1 observe (methods/mesh.cljk) writes —
# same kind as the one above: regenerated, content-addressed, never hand-edited
data/persisted/danjo.revenue.datoms.kotoba.edn
data/persisted/danjo.diet.kotoba.edn
data/persisted/danjo.procurement.kotoba.edn
